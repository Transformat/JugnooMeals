<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFF5BE1B">

    <ImageView
        android:id="@+id/splash_meals_logo"
        android:layout_width="200dp"
        android:layout_height="180dp"
        android:layout_centerInParent="true"
        android:background="@drawable/meals_icon" />


    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="55dp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/login_button_splash"
            android:layout_width="130dp"
            android:layout_height="60dp"
            android:background="@drawable/login_button_shape_splash"
            android:onClick="onClick"
            android:text="LOG IN"
            android:textColor="@drawable/text_color_change_splash"
            android:textSize="22sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/signup_button_splash"
            android:layout_width="130dp"
            android:layout_height="60dp"
            android:layout_marginLeft="45dp"
            android:background="@drawable/signup_button_shape_splash"
            android:onClick="onClick"
            android:text="SIGN UP"
            android:textColor="#F5BE1B"
            android:textSize="22sp"
            android:textStyle="bold" />
    </LinearLayout>
</RelativeLayout>